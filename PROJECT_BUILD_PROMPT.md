# 한국 주식시장(KRX) AI 자동매매 시스템 구축 프롬프트

> 이 문서는 실제로 구현·운영 중인 시스템의 설계를 바탕으로 작성된 "재구축용 상세 명세서"입니다.
> AI 코딩 에이전트(Claude Code 등)에게 그대로 전달하거나, 개발자가 직접 참고해 단계적으로
> 구현할 수 있도록 아키텍처·데이터 모델·전략 로직·안전장치·운영 노하우를 최대한 구체적으로
> 담았습니다. 개인 식별 정보(계정, 경로, 저장소 주소 등)는 모두 제거했습니다.

---

## 1. 프로젝트 개요

**목표**: 한국 주식시장(KRX)을 대상으로, 저사양 상시 구동 기기(구형 스마트폰 등 Termux/리눅스 환경 포함)에서 24시간 돌아가는 AI 기반 반자동 매매 시스템을 구축한다. 사용자 인터페이스는 텔레그램 챗봇이며, 매매 전략 산출과 리스크 관리는 완전 자동화하고 사용자는 알림을 받고 필요시 대화형으로 개입한다.

**핵심 설계 철학**:
- **계좌가 진실, DB는 사본**: 브로커 실계좌 데이터가 항상 우선하고, 로컬 DB는 캐시/이력 저장 용도로만 쓴다. 둘이 어긋나면 반드시 계좌 기준으로 대사(reconcile)한다.
- **모든 전략 파라미터는 DB 하이퍼파라미터로 노출**: 손절선, 트레일링 배수, 배치 크기 등은 코드 재배포 없이 챗봇 명령 한 줄로 즉시 튜닝 가능해야 한다.
- **백테스트로 검증 후 실거래 반영**: 스탑 배수나 배치 크기 같은 "전략 가설"은 반드시 워크포워드 백테스트로 수치 검증한 뒤에만 라이브 코드에 반영한다. 반면 자금 손실로 직결되는 버그(예수금 미차감, 발신자 미검증 등)는 검증 없이 즉시 수정한다 — 이 둘을 구분하는 게 중요하다.
- **저사양 환경 고려**: 무거운 ML 프레임워크 대신 TF-IDF+Naive Bayes 같은 경량 모델을 온디바이스로 돌리고, 로그 파일은 로테이션하며, DB는 SQLite WAL 모드로 충분히 감당한다.

---

## 2. 전체 아키텍처

두 개의 서브시스템으로 구성한다.

| 서브시스템 | 역할 |
|---|---|
| **stock_trader** | 전략 산출 → 주문 집행 → 리스크 관리 → 학습 기반 축적 |
| **secretary** (선택) | 캘린더/할일 관리, 대화 기록 검색 — 매매와 무관한 개인 비서 기능. 없어도 매매 시스템은 독립적으로 동작해야 한다. |

```
┌─────────────────────────────────────────────────────────────────┐
│                    unified_watchdog.py (최상위 프로세스 관리자)    │
│  - 하위 데몬 프로세스 5개를 감시하며 죽으면 즉시 재기동            │
│  - 정시 배치 작업(전략 엔진 08:30, 마감보고 15:40, 공시감시 30분   │
│    간격, 월간 유니버스 확장)을 "정시 일치"가 아닌 "시간 윈도우 +   │
│    당일 1회 가드" 방식으로 스케줄링 (직전 작업 지연에도 안 놓침)   │
└───────┬──────────────────────────────┬───────────────────────────┘
        │ 서브프로세스 실행             │
        ▼                              ▼
┌───────────────┐              ┌────────────────────┐
│ strategy_     │  DB PENDING  │   auto_trader.py   │
│ engine.py     │ ──────────► │  (주문 집행 엔진,   │
│ (1일 1회,     │  신호        │   전용 스레드로     │
│  전략 산출)   │              │   리스크 감시 분리)  │
└───────┬───────┘              └────────┬───────────┘
        │ IPC (비동기 TCP 소켓)          │
        ▼                              ▼
┌─────────────────┐           ┌─────────────────────┐
│ notification_   │           │   broker_factory     │
│ gateway.py      │           │   (증권사 어댑터 추상화)│
│ (이벤트→텔레그램)│           └─────────────────────┘
└─────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  telegram_listener.py (챗봇 데몬)                │
│  사용자 메시지 → 인텐트 라우팅(승인된 발신자만) → 스킬 실행       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 언어 | Python 3.13 | |
| DB | SQLite (WAL 모드) | 단일 사용자, 저사양 기기, 배포 간편성. ORM 없이 얇은 Repository 패턴으로 충분 |
| 시세/재무 데이터 | FinanceDataReader, 네이버 금융 크롤링(BeautifulSoup), DART OpenAPI | 무료 데이터 소스 조합. 크롤링은 구조 변경에 취약하므로 별도 모듈로 격리 |
| ML | scikit-learn (TF-IDF + MultinomialNB) | 인텐트 분류용 경량 모델, 저사양 기기에서 실시간 추론 가능 |
| 알림/UI | Telegram Bot API (long polling) | 무료, 모바일 친화적, 양방향 대화 가능 |
| AI 폴백 | Gemini API (Function Calling) | 로컬 모델이 확신 못 할 때만 호출 — 비용/지연 최소화 |
| 프로세스 간 통신 | asyncio 기반 TCP 소켓(직접 구현) | 로컬 단일 머신이라 Redis/ZeroMQ 같은 별도 브로커 불필요 |
| 데이터 분석 | pandas, numpy | OHLCV 처리, 백테스트 |

**이 규모에서 피해야 할 과잉 설계**: SQLAlchemy 같은 ORM, Redis/ZeroMQ 같은 메시지 브로커, 마이크로서비스 분리. 단일 사용자·단일 기기 운영에는 오버엔지니어링이며 유지보수 부담만 늘린다.

---

## 4. 디렉토리 구조

```
project-root/
├── unified_watchdog.py          # 최상위 프로세스 감시·스케줄러
├── start.sh / stop.sh
├── requirements.txt
├── stock_trader/
│   ├── config.py                # 전역 경로·프로파일 상수 (모든 모듈이 여기서 import)
│   ├── core/
│   │   ├── strategy_engine.py   # 전략 엔진 (오케스트레이션)
│   │   ├── auto_trader.py       # 실시간 주문 집행 + 리스크 감시
│   │   ├── indicators.py        # 순수 함수: RSI/볼린저밴드/ATR/SMA/ADX
│   │   ├── signals.py           # ETF 추세 전략용 진입/청산 판정 순수 함수
│   │   ├── scorer.py            # 멀티팩터 스코어링 (순수 함수, I/O 없음)
│   │   ├── market_features.py   # 기술적 지표 피처 계산 (순수 함수)
│   │   ├── reconciler.py        # 브로커 실계좌 ↔ DB 포지션 대사 로직
│   │   ├── backtest_multifactor.py  # 워크포워드 백테스터 (기술적 코어 검증용)
│   │   ├── factor_analysis.py   # 실거래 학습 기반 팩터 IC 분석 (읽기 전용)
│   │   ├── stock_universe.py    # 종목 유니버스 정의 + 동적 확장분 병합
│   │   ├── universe_feeder.py   # 시총 상위 종목 자동 수집
│   │   ├── summary_trader.py    # 일일 마감 보고 (실계좌 기준)
│   │   ├── trainer.py           # 로컬 NLP 모델 재학습
│   │   └── korean_market_calendar.py  # KRX 휴장일
│   ├── ai/
│   │   ├── local_intent_router.py  # 온디바이스 인텐트 분류 (TF-IDF+NB)
│   │   └── agent_skills.py         # Gemini Function Calling 스킬 정의 + 실행부
│   ├── broker/
│   │   ├── broker_interface.py     # 추상 인터페이스 (ABC): 4개 공통 메서드
│   │   ├── broker_factory.py       # 싱글톤 팩토리, 활성 브로커 목록 관리
│   │   └── adapters/               # 증권사별 구현체 (실 API 1개 이상 + Mock 1개 이상)
│   ├── communication/
│   │   ├── telegram_listener.py    # 챗봇 데몬 (발신자 검증 필수)
│   │   ├── telegram_utils.py       # 메시지/사진 전송 유틸 (Markdown 실패 시 평문 폴백)
│   │   ├── notification_gateway.py # IPC 이벤트 → 텔레그램 변환
│   │   └── ipc_messenger.py        # 비동기 TCP 소켓 Publisher/Listener
│   ├── data/
│   │   ├── db_repository.py        # SQLite 전체 데이터 액세스 계층 (모든 쿼리는 여기로)
│   │   ├── dart_api.py             # 공시/재무제표 API 클라이언트
│   │   ├── dart_financial_scorer.py
│   │   └── dart_disclosure_monitor.py
│   ├── monitoring/
│   │   ├── system_monitor.py       # CPU/메모리/배터리 수집
│   │   ├── system_monitor_loop.py  # 모니터링 데몬
│   │   └── system_trend_reporter.py
│   └── tests/                      # pytest 기반, 실 API 없이 임시 SQLite로 동작
└── README_STRATEGY.md / CODEBASE.md  # 전략 설명서 / 아키텍처 문서 (항상 최신 유지)
```

---

## 5. 데이터베이스 스키마 (SQLite)

`DbRepository` 클래스가 초기화 시 `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE ... ADD COLUMN` 마이그레이션을 자동 수행하도록 만든다 (스키마 변경 시 별도 마이그레이션 도구 없이 앱 재시작만으로 반영).

### 핵심 테이블

**`trade_signals`** — 전략 엔진이 생성한 매매 신호 (PENDING 상태로 적재, 실행 엔진이 소비)
```sql
id, broker_id, ticker, name, action(BUY/SELL/HOLD), quantity,
reason, status(PENDING/DONE/CANCELLED/EXPIRED), created_at, features(JSON)
```
`features`에는 신호 생성 시점의 스코어·기술적 지표·펀더멘털 팩터 전체 스냅샷을 JSON으로 저장한다 (뒤에 나올 "학습 기반"의 핵심 데이터 소스).

**`trade_history`** — 실제 체결 이력. `price`는 반드시 실제 체결가(계좌 재조회로 추정 가능) — **절대 0으로 기록하지 말 것** (아래 6절 함정 참고).

**`portfolio_status`** — 보유 종목 현황. PK는 `(broker_id, stk_cd)`.
```sql
broker_id, stk_cd, stk_nm, rmnd_qty, pur_pric, cur_prc, prft_rt,
max_profit_rate,          -- 트레일링 스탑 기준점 (peak). 브로커가 알 수 없는 "전략 상태"이므로
                           -- 실계좌 대사 시에도 절대 덮어쓰지 않는다.
is_runner,                 -- 부분 익절 후 잔여 물량 플래그 (아래 8절 참고)
out_of_top_streak,         -- 순위 이탈 연속 거래일 수 (교체매도 유예 판정용)
entry_date, entry_price, entry_signal_type, entry_features  -- 진입 스냅샷 (학습 기반, 9절 참고)
```

**`strategy_hyperparams`** — `param_key TEXT PRIMARY KEY, param_value REAL, updated_at`. 모든 전략 파라미터는 여기서 읽는다. 코드에는 "DB 조회 실패 시 폴백 기본값"만 하드코딩한다.

**`market_lockout`** — 서킷브레이커/글로벌 하드스탑 락아웃 상태. `active, since, reason`. 프로세스 재시작에도 상태가 유지되도록 **DB에 영속화**해야 한다 (메모리 변수만으로는 재시작 시 소실됨).

**`stopped_positions`** — 손절 청산된 종목의 쿨다운 추적 (재진입 방지용, `(ticker, stop_date)` PK).

**`trade_outcomes`** — 학습 기반 (9절 참고). 진입 팩터 스냅샷과 실현 청산 결과를 1:1로 연결한 테이블.

**`ohlcv_data`** — 로컬 시계열 캐시. `(ticker, date)` PK, delta 병합 방식으로 매번 전체 재수집하지 않는다.

**부가 테이블**: `account_summary`(계좌 스냅샷), `system_metrics`(하드웨어 상태), `scheduled_tasks`, `training_data`(NLP 피드백).

### 인덱스
자주 필터링되는 컬럼(`trade_signals.status`, `trade_history.timestamp`, `training_data.is_trained` 등)에 인덱스를 반드시 건다.

### 커넥션 관리
```python
@contextmanager
def get_connection(self):
    conn = sqlite3.connect(self.db_path, timeout=30.0)
    conn.execute("PRAGMA journal_mode=WAL;")
    conn.execute("PRAGMA synchronous=NORMAL;")
    conn.execute("PRAGMA busy_timeout=5000;")
    conn.execute("PRAGMA cache_size=-64000;")
    yield conn
    conn.commit()  # 예외 시 rollback
```
**모든 DB 접근은 이 Repository를 통해서만** 이루어져야 한다. 실행 엔진 코드에서 직접 `sqlite3.connect()`를 호출하는 걸 절대 허용하지 말 것 — PRAGMA 설정 누락, 중복 쿼리, 스키마 변경 시 누락 위험이 생긴다.

---

## 6. 브로커 추상화 계층

```python
class BrokerInterface(ABC):
    def get_account_summary(self) -> dict: ...
    def place_order(self, stock_code, quantity, price, side) -> dict: ...
    def cancel_order(self, order_no) -> bool: ...
    def get_current_price(self, stock_code) -> float: ...

class BrokerFactory:
    # 싱글톤 캐싱, get_active_brokers()로 멀티 증권사 동시 지원
```

- 모의투자(MOCK)/실전(REAL) 모드는 별도 설정 섹션(예: `config.ini`의 `BROKER_MOCK`/`BROKER_REAL`)으로 완전히 분리하고, 시크릿 파일은 `.gitignore`에 반드시 포함한다.
- OAuth 토큰 발급 로직에 **레이트리밋(429) 지수 백오프**, **401/토큰만료 자동 재발급**을 구현한다.
- 최소 1개의 Mock 어댑터(인메모리 시뮬레이션)를 만들어 실 API 키 없이도 테스트가 돌아가게 한다.

---

## 7. 전략 엔진 상세 설계 (`strategy_engine.py`)

### 7.1 실행 순서 (1일 1회, 장 시작 전)
1. 하이퍼파라미터 로드 (DB → 실패 시 코드 기본값 폴백)
2. **시장 국면 판독**: 지수(코스피) 50일 이동평균 + ADX(14)로 BULL/BEAR/SIDEWAY 3분류
   - ADX < 20이면 원칙적으로 SIDEWAY이나, 지수가 50일선 대비 +8% 이상이면 추세 상승 중으로 간주해 BULL 적용 (레인지 필터의 함정: ADX만으로 판단하면 완만한 상승 추세를 옆걸음으로 오판할 수 있음)
   - 국면 판독 자체가 실패하면(데이터 부족 등) **보수적으로 BEAR 파라미터를 적용**하는 안전장치를 반드시 넣을 것
3. **서킷브레이커 체크**: 지수가 특정 % 이상 급락하면 신규 매수 잠금
4. **브로커 실계좌 ↔ DB 포지션 대사** (아래 10절) — 대사 실패 시 그날 신호 생성을 중단해야 한다 (어긋난 원장 위에서 매매하면 안 됨)
5. **시장 데이터 수집**: OHLCV + 크롤링(EPS/수급) + 공시 API(재무) + 기술지표 계산
6. **멀티팩터 스코어링**
7. **매도(관리) 신호 생성**: 하드스탑/트레일링/오버슈팅/교체매도/글로벌 하드스탑
8. **매수 신호 생성**: 스코어 상위 N종목 중 진입 조건 충족 종목만
9. DB 저장 + IPC로 알림 게이트웨이에 이벤트 발행

### 7.2 멀티팩터 스코어링 설계
팩터를 카테고리별로 나누고 **가중합이 정확히 1.0이 되도록 설계**한다 (검증 가능한 불변식으로 만들어 두면 나중에 가중치를 조정할 때 실수를 방지한다).

| 팩터 카테고리 | 가중치 예시 | 데이터 소스 |
|---|---|---|
| EPS 성장률 | 0.15 | 재무 포털 크롤링 |
| 섹터/업종 모멘텀 | 0.20 | 동일 업종 종목들의 5일 평균 수익률로 산출 |
| 매출 성장률 (공시 기반) | 0.15 | 공시 API |
| 영업이익 성장률 (공시 기반) | 0.20 | 공시 API |
| 재무건전성 (부채비율+현금흐름) | 0.05 | 공시 API |
| 수급 (기관/외국인 순매수) | 0.25 | 크롤링 + 대량보유 지분변동 보너스 |

추가 보너스/페널티(가산점 방식, 가중치 합계에는 안 넣고 별도 조정):
- 상대 모멘텀(지수 대비 90일 초과수익) 보너스 (상한 있음)
- 이동평균 정배열(20>60>120일) 가점 / 120일선 하회 시 큰 감점
- 배당수익률이 임계치 이상이면 보너스
- 부채비율이 지나치게 높으면 페널티
- 변동성 수축 패턴(볼린저밴드 하단 근접 + 거래량 급감) 가점 — 되돌림 진입의 신호로 활용

**팩터별 함수는 순수 함수로 분리**하고(`scorer.py`), I/O(크롤링·API 호출)는 별도 데이터 프로바이더 모듈(`market_features.py`, 네이버 크롤링 전용 모듈 등)에 격리한다. 이렇게 해야 백테스트에서 데이터 수집 없이 스코어링 로직만 재사용할 수 있다.

### 7.3 매수 신호 — 진입 유형 2가지
- **역추세(평균회귀)**: RSI가 국면별 임계치 이하이거나 볼린저밴드 하단 이하일 때. 단, "RSI만 조건 충족하고 하락세가 아직 안 멈췄으면"(MACD 데드크로스 지속 등) 스킵하는 필터를 반드시 넣는다 — 안 그러면 "떨어지는 칼날"을 계속 잡게 된다.
- **추세추종**: BULL/SIDEWAY 국면에서만, 이동평균 정배열 + RSI가 과매수도 과매도도 아닌 중립~강세 구간 + 지수 대비 상대강도 양(+)일 때.

### 7.4 자금 배분 (포지션 사이징)
```
score_ratio = (해당종목스코어 / 상위N종목스코어합) × N   # 평균 1.0이 되도록 정규화
size_factor = clamp(0.5, 1.5, 기준변동성 / 종목ATR%) × 전략유형가중치
목표비중 = 종목당_목표비중 × score_ratio × size_factor
매수금액 = min(계좌총자산 × 목표비중, 가용예수금 × 1회주문최대비율, 잔여예수금)
```
- `종목당_목표비중`, `1회주문최대비율`은 **반드시 DB 하이퍼파라미터로 노출**한다 — 소액 계좌에서는 `1회주문최대비율`이 실제 병목이 되는 경우가 흔하다 (예: 100만원 계좌에서 20% 캡이면 건당 20만원, 대부분의 대형주를 1주도 못 사는 상황이 생길 수 있음). 코드 상수로 고정해두면 나중에 챗봇 명령으로 튜닝할 수 없게 되니 처음부터 하이퍼파라미터화할 것.
- 루프를 돌며 종목을 순차 매수할 때 **잔여 예수금을 실시간으로 차감**해야 한다. 그렇지 않으면 여러 매수 신호가 같은 예수금을 중복 소진해 예수금 초과 주문이 발생한다.

### 7.5 매도(리스크 관리) — 판정 순서와 각각의 함정

**우선순위(위에서부터 순서대로 판정, 하나라도 걸리면 즉시 그 사유로 청산)**:

1. **글로벌 하드스탑**: 계좌 전체 미실현 손익률이 한도 이하면 전량 청산 + 락아웃.
   - ⚠️ 함정: 손실률의 분모를 "보유 종목 매입금 합"으로 잡으면, 현금 비중이 높은 상태에서 소수 포지션만 손실이어도 과민하게 전량 청산될 수 있다. **계좌 총자산(현금 포함) 대비**로 계산하는 게 낫다.
2. **개별 종목 하드스탑**: 변동성(ATR) 기반 동적 손절선. 고정 %가 아니라 `-clamp(하한, 상한, ATR배수 × ATR%)` 형태로 만들어 변동성 큰 종목은 손절선을 넓히고 작은 종목은 좁힌다.
3. **트레일링 스탑(Chandelier Exit)**: 진입 후 달성한 최고 수익률(peak) 대비 ATR 배수만큼 하락 시 청산. 배수는 2.5~3.0 근처가 무난하다 (1.5배수처럼 타이트하게 잡으면 장중 흔들기에 과도하게 털린다 — 실제로 백테스트로 1.5→2.5 변경 시 총수익이 유의미하게 개선되는 걸 확인한 사례가 있다).
   - ⚠️ **매우 중요한 함정**: 만약 "일별 배치 판정"(전략 엔진)과 "장중 실시간 감시"(주문 실행 엔진)를 별도 프로세스로 나눈다면, **반드시 동일한 DB 하이퍼파라미터 키를 공유**하게 만들어야 한다. 두 경로가 각자 다른 배수를 하드코딩하면 같은 포지션에 대해 서로 다른 기준으로 청산 판정을 내리는 이중 감시 비일관 상태가 생긴다.
4. **오버슈팅 익절**: RSI 과매수 진입 또는 볼린저밴드 상단 돌파 시. **전량 익절보다 "부분 익절 + 잔여 물량 러너 전환"이 백테스트상 더 유리한 경우가 많다** (추세가 이어지면 잔여 물량이 더 먹고, 트레일링/하드스탑으로만 관리되므로 오버슈팅 재발동으로 인한 조기 이탈이 없어짐). 부분 익절 비율도 하이퍼파라미터화한다.
5. **교체매도(리밸런싱)**: 스코어 순위에서 탈락한 보유 종목을, 수익이 미미할 때 상위 신규 종목으로 교체.
   - ⚠️ 함정: 순위는 노이즈가 많다. **최소 보유일수 미경과 시 유예**, **순위 이탈이 며칠 연속 지속될 때만 교체**(유예일수) 같은 규율을 넣지 않으면 잦은 회전으로 거래비용만 새는 "휩쏘"가 발생한다. 이 두 규율 모두 백테스트로 검증했을 때 승률/샤프 개선이 뚜렷했다.

### 7.6 국면별 방어
- BEAR 국면에서는 추세추종 진입을 완전히 차단(BULL/SIDEWAY 한정)하고, 역추세 매수도 비중을 배수(예: 0.5배, 0이면 전면 중지)로 축소하는 하이퍼파라미터를 둔다.

### 7.7 ETF 추세추종 프로파일 (선택)
개별주 멀티팩터 전략과 별도로, 지수 추종 ETF(코스피200 등) 대상의 단순 추세추종 프로파일(장기 이평선 상회 시 보유, 샹들리에 스탑 청산, 재진입 쿨다운)을 분리된 프로파일로 둘 수 있다. 자금 관리 파라미터(목표비중 등)는 개별주 프로파일과 **분리해서 관리**한다 — 한쪽을 튜닝해도 다른 쪽 검증되지 않은 파라미터가 같이 바뀌는 걸 방지.

---

## 8. 실시간 매매 실행 엔진 (`auto_trader.py`)

### 8.1 구조: 두 개의 독립 루프
- **메인 루프**: PENDING 신호를 폴링해 순차 실행 (30초~1분 간격 권장)
- **리스크 감시 전용 스레드**: 보유 종목 Hard/Trailing Stop만 별도 스레드에서 1분 간격 체크

⚠️ **함정**: 둘을 하나의 루프에 합치면, API 레이트리밋(429) 발생 시 지수 백오프로 수십 초씩 sleep 하는 동안 트레일링 스탑 감시가 통째로 멈춘다. 급락장에서 손절이 지연될 수 있으므로 **반드시 분리**한다.

### 8.2 스마트 매수 집행
- 신호에 목표가(눌림목 대기가)가 있으면, 장중에는 목표가 도달까지 대기
- 장 마감 임박(예: 14:30 이후)에는 목표가 미도달이어도 강제 집행하되, **추격 허용 폭(하이퍼파라미터)을 초과하면 매수를 포기**한다. 폭 제한 없이 무조건 강제 집행하면 엣지 없는 고가 추격 매수가 된다.
- 현재가 조회가 실패하면 **매수를 보류하고 다음 루프에서 재시도**한다. "일단 진행"으로 처리하면 가격 검증 없이 시장가 주문이 나갈 위험이 있다.

### 8.3 예수금 관리 (자금 손실 직결 — 반드시 정확히 구현)
- 한 루프 안에서 여러 매수 신호를 처리할 때, **매수 성공 직후 예수금을 즉시 차감**해서 다음 신호가 이미 쓰인 돈을 다시 쓰지 않게 한다.
- 예수금이 계획보다 부족하면, **앞 순번 신호가 잔여 예수금을 전부 가져가지 않도록** 남은 매수 신호 수로 공평하게 나눠 배정한다.

### 8.4 체결가 기록 (자금 손실 직결)
- 시장가 주문은 응답에 정확한 체결가가 없는 경우가 많다. **주문 접수 성공 = 체결 완료가 아니다.** 접수 성공 후 계좌를 재조회해 실제 평균단가를 확인하고, 그 값을 이력에 기록한다. 실패 시에만 주문 직전 현재가로 추정 기록한다.
- ⚠️ **절대 체결가를 0으로 기록하지 말 것** — 수익률 분석, 학습 데이터, 마감 보고 전부가 이 값을 기준으로 계산되므로 0이면 모든 후속 분석이 무의미해진다.

### 8.5 서킷브레이커 상태 영속화
지수 급락 시 매수 잠금(`market_halt`)을 메모리 변수로만 관리하면, 워치독이 프로세스를 재시작하는 순간 락아웃이 풀려버린다. **DB에 상태(활성 여부·사유·시작시각)를 기록**하고 프로세스 시작 시 복원한다.

### 8.6 텔레그램 발신자 검증 (보안 — 최우선 필수)
챗봇이 받는 모든 메시지에 대해 **발신자 chat_id가 승인된 것과 일치하는지 반드시 확인**한다. 검증 없이 모든 메시지를 처리하면, 봇 사용자명을 아는 누구나 매수/매도 주문을 실행시킬 수 있는 심각한 보안 결함이 된다.

---

## 9. 학습 기반 (실거래 데이터로부터 배우기)

**설계 목표**: 매수 시점의 팩터 스냅샷(스코어, 기술적 지표, 펀더멘털)을 종목이 매도될 때까지 보존했다가, 매도 체결 시 실현 수익률과 묶어 하나의 행(row)으로 저장한다. 이 데이터가 쌓이면 "어떤 팩터가 실제로 수익을 예측했는가"를 사후 분석할 수 있다.

### 9.1 진입 스냅샷 보존
- 포지션 테이블에 `entry_date/entry_price/entry_signal_type/entry_features` 컬럼을 추가한다.
- 매수 체결 시, **이미 진입 스냅샷이 있으면(추가매수) 덮어쓰지 않는다** — 트레일링 스탑의 peak 값과 동일한 원칙: "전략 상태는 브로커가 알 수 없는 우리 쪽 진실"이므로 최초 진입 근거를 보존해야 한다.

### 9.2 실현 결과 기록
매도 체결마다 `trade_outcomes` 테이블에 1행씩 남긴다:
```
entry_date, entry_price, entry_signal_type, entry_features(JSON),
exit_date, exit_price, exit_reason, quantity, return_pct, holding_days,
position_closed  -- 0: 부분 익절(포지션 지속), 1: 완전 청산
```
- 오버슈팅 **부분** 익절은 `position_closed=0`으로 기록하고 진입 스냅샷은 그대로 둔다(포지션이 계속 살아있으므로).
- 완전 청산일 때만 결과를 기록한 **뒤에** 진입 스냅샷을 초기화한다(순서 중요 — 먼저 초기화하면 학습 데이터 연결이 끊긴다).
- 여러 매도 경로(배치 신호 실행, 실시간 감시발 긴급 청산)가 있다면 **모든 경로에서 동일하게** 이 기록 로직을 호출해야 한다.

### 9.3 팩터 분석 (읽기 전용)
`trade_outcomes`를 읽어 팩터별 실현수익률 상관(Information Coefficient, 스피어만 상관 추천)을 계산하는 별도 스크립트를 만든다:
- 청산사유별/진입유형별/시장국면별 평균 수익률
- 팩터별 IC (표본이 적으면(예: 30건 미만) 신뢰 불가 경고를 반드시 표시)

**중요한 경계**: 이 분석은 **읽기 전용이며 스코어 가중치나 하이퍼파라미터를 자동으로 바꾸지 않는다.** 표본이 충분히 쌓인 뒤 사람이 결과를 보고 판단해서 조정하는 걸 원칙으로 한다. 실거래 자금이 걸린 시스템에서 "학습"이 자동으로 전략 로직을 변형하게 만드는 건 별도의 신중한 설계와 승인이 필요한 영역이므로, 최초 구축 단계에서는 반드시 사람이 개입하는 구조로 만들 것.

---

## 10. 계좌 대사(Reconciliation)

브로커 실계좌와 로컬 DB 포지션 기록을 주기적으로 비교해 4가지 시나리오를 처리한다:

| 시나리오 | 처리 |
|---|---|
| DB에는 있으나 계좌엔 없음 (유령 포지션) | DB에서 삭제 |
| 계좌엔 있으나 DB에 없음 (미기록 포지션) | DB에 신규 등록 (peak/entry는 현재 평단가로 초기화) |
| 수량/단가 불일치 | 계좌 값으로 갱신 (단, 전략 상태인 peak·entry는 절대 덮어쓰지 않음) |
| 계좌가 완전히 비어있는데 DB에 다수 포지션 존재 (모의계좌 리셋 등) | 전량 정리, 단 쿨다운 기록은 보존 |

⚠️ **매우 중요한 함정**: 브로커 API가 일시 장애일 때 일부 어댑터는 `None` 대신 **모든 필드가 0인 빈 응답 객체**를 반환하도록 구현되곤 한다. 이런 응답을 "계좌가 완전히 비어있다"로 오인하면, API가 잠깐 끊겼을 뿐인데 정상 보유 포지션을 전량 삭제해버리는 대형 버그가 된다. **응답에 자산/예수금 필드 자체는 있는데 전부 0이고 보유도 0건이면, API 실패 폴백 응답으로 간주하고 대사를 중단**하는 가드를 반드시 넣을 것.

---

## 11. 텔레그램 챗봇 인터페이스

### 11.1 인텐트 라우팅 (비용 최소화를 위한 다단계 폴백)
```
1. 상태 머신 체크 (확인 대기 / 선택 대기 상태면 그 흐름 우선 처리)
       ↓ 해당 없음
2. 온디바이스 로컬 모델 (TF-IDF+NaiveBayes, 신뢰도 임계값 이상만 즉시 실행)
       ↓ 확신도 낮음
3. DB 과거 피드백과의 유사도 매칭 (문자 집합 겹침이 아닌 n-gram 자카드 유사도 권장 — 
   단순 겹침은 무관한 문장도 쉽게 오매칭됨)
       ↓ 그래도 애매
4. 외부 LLM API Function Calling (가장 정확하지만 비용/지연 발생, 최후 수단으로만 사용)
```
- 사용자 피드백(맞다/아니다, 번호 선택)이 들어오면 즉시 로컬 모델을 비동기 재학습시켜 다음 메시지부터 반영한다.
- 긍정/부정 판정 정규식은 **반드시 문장 전체 매칭(끝 앵커 `$`)**으로 작성한다. 앵커 없이 "응"만으로 매칭하면 "응, 근데 어제 얼마 벌었어?" 같은 문장이 긍정으로 오판된다.

### 11.2 파라미터 실시간 튜닝 명령
`[SYSTEM_UPDATE] KEY=VALUE KEY2=VALUE2` 형태의 메시지를 파싱해 `strategy_hyperparams` 테이블을 즉시 갱신하는 핸들러를 둔다. 이게 "코드 재배포 없이 전략 튜닝"의 핵심 메커니즘이다.

### 11.3 알림 메시지 안정성
Markdown 파싱 실패(종목명에 특수문자가 섞이는 경우 등)로 알림이 통째로 유실되지 않도록, 전송 실패(400) 시 서식 없는 평문으로 재전송하는 폴백을 넣는다.

---

## 12. 백테스트 프레임워크

**목적**: 전략 로직 변경(스탑 배수, 배치 크기, 진입 조건 등)을 라이브에 반영하기 전에 수치로 검증한다.

**설계 원칙**:
- **프로덕션 코드 경로를 그대로 재사용**한다 (`indicators.py`, `scorer.py`, `market_features.py`의 순수 함수들). 백테스트 전용으로 로직을 새로 작성하면 라이브와 괴리가 생긴다.
- **워크포워드 방식**(매일 순차적으로 그날까지의 데이터만 사용)으로 look-ahead bias를 원천 차단한다. "신호 발생일의 다음 거래일에 체결"처럼 반드시 시차를 둔다.
- 수수료·거래세·슬리피지를 반영한다.
- **한계를 문서에 명시할 것**: 만약 재무제표/수급 같은 팩터가 과거 시점(point-in-time) 데이터로 구할 수 없다면, 그 팩터들은 중립값으로 고정하고 "이 백테스트는 기술적 코어만 검증한다"는 사실을 스크립트 docstring에 명확히 남긴다. 전체 스코어의 상당 부분(예: 70% 이상)이 검증 불가한 펀더멘털 팩터에 의존한다면, 백테스트 성과가 실거래 성과를 과대/과소평가할 수 있다는 리스크를 늘 인지해야 한다.
- 결과 리포트에 총수익률/CAGR/MDD/샤프비율/승률/손익비/청산사유별 평균수익률과, 동일 기간 지수 벤치마크를 함께 출력한다.

**실험 방법론**: 파라미터를 하나씩 바꿔가며 A/B 비교하고, 마지막에 유의미한 조합을 결합 테스트한다. 결합 시 개별 실험에서는 안 보이던 상호작용 효과(예: 배치를 키우면 교체 회전이 늘어나 유예 규율의 효과가 커지는 식)가 나타날 수 있으므로 개별 실험 결과만으로 결합 성과를 추정하지 말고 반드시 결합도 실제로 돌려본다.

---

## 13. 프로세스 관리 (워치독)

```python
PROCESSES = [
    {"path": "core/auto_trader.py", "name": "주식 자동매매"},
    {"path": "communication/telegram_listener.py", "name": "텔레그램 리스너"},
    {"path": "monitoring/system_monitor_loop.py", "name": "시스템 모니터링"},
    {"path": "communication/notification_gateway.py", "name": "알림 게이트웨이"},
]
```
- 10초 주기로 각 프로세스 생존 확인, 죽어있으면 즉시 재기동.
- 정시 배치 작업(전략 엔진 실행 등)은 **"현재 시각 == 목표 시각"으로 정확히 일치할 때만 실행**하는 방식을 쓰지 말 것 — 직전 작업이 1분 이상 걸리면 그 순간을 통째로 놓친다. **"목표 시각부터 N분 윈도우 이내 + 당일 아직 미실행"** 조건으로 판정한다.
- 장시간 실행될 수 있는 서브프로세스 호출에는 **반드시 timeout을 건다** — 안 그러면 하위 프로세스가 멈췄을 때 워치독 전체가 같이 멈춘다.
- 재시작마다 새로 여는 로그 파일 핸들을 이전 것과 교체할 때 반드시 닫는다 (안 그러면 재시작 루프에 빠졌을 때 파일 디스크립터가 고갈된다).

---

## 14. 외부 데이터 소스 연동 노트

- **시세**: 무료 라이브러리(FinanceDataReader 등)로 OHLCV/시총 조회, 실패 시 크롤링으로 폴백.
- **재무제표/공시**: 공공 공시 API(한국은 DART OpenAPI)로 재무제표·배당·대주주 지분변동 조회. 종목코드 ↔ 내부 고유번호 매핑은 캐싱(예: 30일)해서 API 호출을 아낀다.
- **실시간 지수**: 공식 API가 무겁거나 제한적이면, 포털 사이트의 폴링 API를 REST로 직접 호출하는 우회 방법도 실무적으로 쓸 만하다(단, 구조 변경에 취약함을 인지).
- **크롤링 의존 모듈은 반드시 별도 파일로 격리**한다. 포털 HTML 구조가 바뀌면 그 모듈만 고치면 되게. 크롤링 실패가 전체 전략 엔진을 죽이지 않도록 예외 처리와 폴백값을 촘촘히 넣는다.
- 자동매매 유니버스 확장(신규 종목 편입)은 재무 필터(매출/영업이익/부채비율 등 최소 기준)를 걸어 월 1회 정도의 저빈도 배치로 처리한다.

---

## 15. 안전장치 체크리스트 (실거래 투입 전 필수 확인)

아래 항목은 실제 개발 과정에서 하나씩 사고를 치거나 칠 뻔했던 것들을 정리한 것이다. 새로 구축한다면 처음부터 아래를 전부 반영해서 시작할 것을 강력히 권한다.

- [ ] 텔레그램 발신자 검증 (chat_id 화이트리스트)
- [ ] 매수 루프 내 예수금 실시간 차감
- [ ] 현재가 조회 실패 시 매수 보류(진행 금지)
- [ ] 체결가 0원 기록 금지 (계좌 재조회 기반 추정)
- [ ] 리스크 감시(하드스탑/트레일링)와 주문 집행을 별도 스레드/루프로 분리
- [ ] 서킷브레이커·글로벌 락아웃 상태를 DB에 영속화 (재시작 대비)
- [ ] 트레일링/하드스탑 배수를 여러 실행 경로(배치/실시간)가 동일한 DB 파라미터로 공유
- [ ] 브로커 API 실패 시 "전부 0" 응답을 계좌 리셋으로 오판해 정상 포지션을 삭제하지 않도록 가드
- [ ] 워치독의 정시 배치 실행에 timeout 설정 + "윈도우+당일 1회" 스케줄링
- [ ] 모든 raw SQL은 Repository 계층 하나로 집중 (실행 코드에서 직접 커넥션 열지 않기)
- [ ] 긍정/부정 판정 정규식에 끝 앵커 적용
- [ ] Markdown 알림 전송 실패 시 평문 폴백
- [ ] 시크릿(API 키, 계좌번호, 봇 토큰)은 전부 `.gitignore` 처리, 저장소에 커밋된 적 없는지 재확인

---

## 16. 테스트 전략

- pytest + 임시(tmp_path) SQLite DB로 Repository 계층의 함수 단위 테스트를 촘촘히 작성한다 (진입 스냅샷 보존, 부분/완전 청산 분기, 대사 시나리오 4가지 등 — "이렇게 짜지 않으면 자금이 샌다"는 지점일수록 반드시 테스트로 고정).
- 브로커는 `unittest.mock.MagicMock`으로 대체해 실 API 없이 실행 경로(성공/실패/부분체결)를 검증한다.
- 외부 의존성(psutil, mcp, google API 클라이언트 등)이 없는 개발 환경에서도 임포트 테스트가 깨지지 않도록, 플랫폼 분기로 `MagicMock`을 `sys.modules`에 주입하는 패턴을 활용할 수 있다.
- 코드 변경 후 회귀 스위트 전체를 돌리는 습관을 들인다 — 특히 DB 스키마나 Repository 메서드를 바꿀 때는 이를 사용하는 다른 모든 모듈의 테스트가 통과하는지 반드시 확인.

---

## 17. 모의투자 → 실전 전환 체크리스트

캘린더 기한이 아니라 아래 **조건 충족 여부**로 판단한다:

1. 워치독이 사람 개입 없이 최소 1개월 사이클(월간 배치까지 포함)을 무중단 통과
2. 하드스탑/서킷브레이커가 실제로(또는 의도적 트리거로) 한 번은 끝까지 작동해 알림까지 확인됨
3. 학습 기반(9절)에 실현 거래가 최소 30건 이상 쌓여 통계적으로 참고 가능한 수준
4. 최근 몇 주간 "자금 손실로 이어질 수 있는 버그"가 새로 발견되지 않음
5. 실전 투입 예정 자금 규모에서 배치 파라미터(종목 수·비중·1회주문한도)가 실제로 의도한 분산을 만들어내는지 사전 계산으로 확인 (소액 계좌는 낱주 매매 제약 때문에 설계된 분산이 무너지기 쉬움 — 자금 규모가 작을수록 종목 수를 줄이고 종목당 비중은 늘리는 식으로 파라미터를 별도로 맞춘다)
6. 모의/실전 API 모드 전환(토큰 발급, 레이트리밋 차이) 검증 완료

**권장 전환 방식**: 한 번에 전액 전환하지 말고, 계획 자금의 일부(예: 10~20%)로 먼저 실전을 돌리며 모의투자도 병행 — 실전이 안정적으로 돌고 모의투자 결과와 크게 어긋나지 않으면 점진적으로 자금을 늘린다.

---

## 부록: 구현 순서 제안

처음부터 전부 만들려 하지 말고 아래 순서로 단계적으로 구축할 것을 권장한다.

1. Repository 계층 + 기본 스키마 + Mock 브로커 어댑터
2. 순수 함수 지표 모듈(indicators) + 단위 테스트
3. 전략 엔진(신호 생성까지만, 실행은 아직 없이 DB 적재까지)
4. 텔레그램 알림 전송(단방향)
5. 실행 엔진(auto_trader) — Mock 브로커로 먼저 전체 파이프라인 검증
6. 챗봇 양방향 인터랙션 + 인텐트 라우팅
7. 리스크 관리 고도화(트레일링/오버슈팅/교체매도 규율) — 이 단계부터 백테스터를 만들어 병행 검증
8. 계좌 대사 로직
9. 워치독/프로세스 관리
10. 학습 기반(trade_outcomes) — 실거래 데이터가 쌓이기 시작하는 시점에 맞춰 마지막에 붙여도 무방

각 단계마다 테스트를 먼저 작성하고, 15절 체크리스트 항목이 관련되는 단계에서는 바로바로 반영해서 나중에 몰아서 고치는 일이 없도록 한다.
